/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "limxqzfphzeqaxtazeyi.supabase.co" },
    ],
  },
};

export default nextConfig;
